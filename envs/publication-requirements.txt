# Direct dependencies pinned for the source/binary-bound publication session.
# The session also records `pip freeze` so all resolved transitive versions are retained.
build==1.5.0
nanobind==2.13.0
numpy==2.4.6
mpmath==1.3.0
piquasso==8.0.1
python-flint==0.9.0
thewalrus==0.22.0
